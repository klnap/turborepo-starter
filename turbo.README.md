# `turbo.json`

Task graph and cache configuration for this monorepo.

**Read:** [docs/turborepo.md](docs/turborepo.md) — what each task does, CLI vs Turbo vs `package.json`, Docker `turbo prune`, adding tasks.

Workspace overrides: `apps/web/turbo.json` (`extends: ["//"]`).
